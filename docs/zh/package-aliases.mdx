---
title: 包别名
description: "已注册的防域名抢注别名及其工作原理"
icon: copy
---

## 官方包

npm 上的规范包名为 **`failproofai`**：

```bash
npm install -g failproofai
# or
bun add -g failproofai
```

---

## 为什么我们持有这些别名

域名抢注（Typosquatting）是一种常见的供应链攻击手段——恶意攻击者会注册一个与热门包名仅差一个按键的包名。不小心输错安装命令的用户，最终会以完整的系统权限运行攻击者控制的代码——这正是 Failproof AI 所要防范的威胁。

为了消除这一攻击面，**我们在 npm 上抢先注册了 `failproofai` 的所有常见拼写错误及格式变体**。这些名称均无法被第三方注册。每一个都是一个轻量代理包，安装后会委托给真正的 `failproofai` 包执行。

---

## 已注册的别名

**格式变体** — "failproof ai" 的不同书写方式：

| 包名 | 状态 |
|---------|--------|
| `failproof` | ✅ 已发布 |
| `failproof-ai` | ⏳ 等待 npm 审批 |
| `fail-proof-ai` | ⏳ 等待 npm 审批 |
| `failproof_ai` | ⏳ 等待 npm 审批 |
| `fail_proof_ai` | ⏳ 等待 npm 审批 |
| `fail-proofai` | ⏳ 等待 npm 审批 |

**`failprof*` 拼写错误** — "proof" 中少了一个 `o`：

| 包名 | 状态 |
|---------|--------|
| `failprof` | ✅ 已发布 |
| `failprof-ai` | ✅ 已发布 |
| `failprofai` | ⏳ 等待 npm 审批 |
| `fail-prof-ai` | ⏳ 等待 npm 审批 |
| `failprof_ai` | ⏳ 等待 npm 审批 |

**`faliproof*` 拼写错误** — `a` 和 `i` 位置互换：

| 包名 | 状态 |
|---------|--------|
| `faliproof` | ✅ 已发布 |
| `faliproof-ai` | ✅ 已发布 |
| `faliproofai` | ⏳ 等待 npm 审批 |

> **为什么显示"等待审批"？** npm 的反垃圾政策会屏蔽那些在去除标点符号并执行相似度检查后，与已有包名规范化结果相同的包名。我们已联系 npm 支持团队，申请以防抢注为目的保留这些名称，获批后即可激活。

您可以通过以下命令验证任意已发布的别名是否由我们持有：

```bash
npm info failproof
# Look for: "ExosphereHost Inc." in the maintainers field
```

---

## 别名的工作原理

每个别名包：

1. 将 `failproofai` 列为依赖项——这样安装时会运行真正的包（包括其 `postinstall` 钩子设置）
2. 暴露一个与自身同名的二进制文件（例如 `failprof-ai`），将所有参数代理转发至 `failproofai` 二进制文件

该代理仅是一个两行的 Node 脚本，没有任何逻辑处理，不发起网络请求，也不进行任何超出 `failproofai` 本身范围的数据收集。

---

## 如果您发现我们遗漏了某个名称

请在 [exospherehost/failproofai](https://github.com/exospherehost/failproofai/issues) 提交 Issue，我们会及时注册该名称。