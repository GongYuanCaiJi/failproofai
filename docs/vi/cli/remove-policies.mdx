---
title: Gỡ cài đặt policies
description: "Xóa các hook entries từ settings của Claude Code"
---

```bash
failproofai policies --uninstall [policy-names...] [options]
```

Xóa các hook entries của failproofai từ `settings.json` của Claude Code.

Bí danh: `failproofai p -u`

## Options

| Flag | Mô tả |
|------|-------------|
| `--scope user` | Xóa từ cài đặt toàn cục (mặc định) |
| `--scope project` | Xóa từ cài đặt dự án |
| `--scope local` | Xóa từ cài đặt cục bộ |
| `--scope all` | Xóa từ tất cả các phạm vi cùng lúc |
| `--custom` / `-c` | Xóa `customPoliciesPath` khỏi cấu hình |

## Hành vi

- **Không có tên policy** - xóa tất cả các hook entries của failproofai từ tệp cài đặt
- **Tên cụ thể** - vô hiệu hóa các policies đó nhưng giữ lại hooks đã cài đặt

## Ví dụ

```bash
# Xóa tất cả hooks toàn cục
failproofai policies --uninstall

# Vô hiệu hóa một policy cụ thể (giữ lại hooks đã cài đặt)
failproofai policies --uninstall block-sudo

# Xóa hooks khỏi mọi phạm vi
failproofai policies --uninstall --scope all

# Xóa đường dẫn custom policies
failproofai policies --uninstall --custom
```