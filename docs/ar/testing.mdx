---
title: الاختبار
description: "اختبارات الوحدة واختبارات E2E ومساعدات الاختبار"
icon: flask-vial
---

يتضمن failproofai مجموعتي اختبار: **اختبارات الوحدة** (سريعة، محاكاة) و**اختبارات end-to-end** (استدعاءات subprocess حقيقية).

---

## تشغيل الاختبارات

```bash
# تشغيل جميع اختبارات الوحدة مرة واحدة
bun run test:run

# تشغيل اختبارات الوحدة في وضع المراقبة
bun run test

# تشغيل اختبارات E2E (يتطلب إعداد - انظر أدناه)
bun run test:e2e

# التحقق من النوع بدون بناء
bunx tsc --noEmit

# التحقق من الأسلوب
bun run lint
```

---

## اختبارات الوحدة

تقع اختبارات الوحدة في `__tests__/` وتستخدم [Vitest](https://vitest.dev) مع `happy-dom`.

```text
__tests__/
  hooks/
    builtin-policies.test.ts      # منطق السياسة لكل سياسة مدمجة
    hooks-config.test.ts          # تحميل الإعدادات ودمج النطاق
    policy-evaluator.test.ts      # حقن المعاملات وترتيب التقييم
    custom-hooks-registry.test.ts # سجل globalThis - إضافة/الحصول/مسح
    custom-hooks-loader.test.ts   # محمل ESM والواردات المتعدية ومعالجة الأخطاء
    manager.test.ts               # عمليات التثبيت/الإزالة/القائمة
  components/
    sessions-list.test.tsx        # مكون قائمة الجلسات
    project-list.test.tsx         # مكون قائمة المشاريع
    ...
  lib/
    logger.test.ts
    paths.test.ts
    date-filters.test.ts
    telemetry.test.ts
    ...
  actions/
    get-hooks-config.test.ts
    get-hook-activity.test.ts
    ...
  contexts/
    ThemeContext.test.tsx
    AutoRefreshContext.test.tsx
```

### كتابة اختبار وحدة لسياسة

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { getBuiltinPolicies } from "../../src/hooks/builtin-policies";
import { allow, deny } from "../../src/hooks/policy-types";

describe("block-sudo", () => {
  const policy = getBuiltinPolicies().find((p) => p.name === "block-sudo")!;

  it("denies sudo commands", () => {
    const ctx = {
      eventType: "PreToolUse" as const,
      payload: {},
      toolName: "Bash",
      toolInput: { command: "sudo apt install nodejs" },
      params: { allowPatterns: [] },
    };
    expect(policy.fn(ctx)).toEqual(deny("sudo command blocked by failproofai"));
  });

  it("allows non-sudo commands", () => {
    const ctx = {
      eventType: "PreToolUse" as const,
      payload: {},
      toolName: "Bash",
      toolInput: { command: "ls -la" },
      params: { allowPatterns: [] },
    };
    expect(policy.fn(ctx)).toEqual(allow());
  });

  it("allows patterns in allowPatterns", () => {
    const ctx = {
      eventType: "PreToolUse" as const,
      payload: {},
      toolName: "Bash",
      toolInput: { command: "sudo systemctl status nginx" },
      params: { allowPatterns: ["sudo systemctl status"] },
    };
    expect(policy.fn(ctx)).toEqual(allow());
  });
});
```

---

## اختبارات end-to-end

تستدعي اختبارات E2E ملف failproofai الثنائي الفعلي كـ subprocess، وتنقل حمولة JSON إلى stdin، وتؤكد على مخرجات stdout وكود الخروج. يختبر هذا مسار التكامل الكامل الذي يستخدمه Claude Code.

### الإعداد

تشغل اختبارات E2E الملف الثنائي مباشرة من مصدر المستودع. قبل التشغيل الأول، قم بإنشاء حزمة CJS التي تستخدمها ملفات الخطافات المخصصة عند الاستيراد من `'failproofai'`:

```bash
bun build src/index.ts --outdir dist --target node --format cjs
```

ثم شغل الاختبارات:

```bash
bun run test:e2e
```

أعد بناء `dist/` كلما غيرت API الخطاف العام (`src/hooks/custom-hooks-registry.ts` أو `src/hooks/policy-helpers.ts` أو `src/hooks/policy-types.ts`).

### هيكل اختبار E2E

```text
__tests__/e2e/
  helpers/
    hook-runner.ts      # استدعاء الملف الثنائي، نقل حمولة JSON، اجمع كود الخروج + stdout + stderr
    fixture-env.ts      # دلائل مؤقتة معزولة لكل اختبار مع ملفات الإعدادات
    payloads.ts         # مصانع حمولة دقيقة حسب Claude لكل نوع حدث
  hooks/
    builtin-policies.e2e.test.ts   # كل سياسة مدمجة مع subprocess حقيقي
    custom-hooks.e2e.test.ts       # تحميل الخطافات المخصصة والتقييم
    config-scopes.e2e.test.ts      # دمج الإعدادات عبر المشروع/المحلي/العام
    policy-params.e2e.test.ts      # حقن المعاملات لكل سياسة معاملية
```

### استخدام مساعدات E2E

**`FixtureEnv`** - بيئة معزولة لكل اختبار:

```typescript
import { createFixtureEnv } from "../helpers/fixture-env";

const env = createFixtureEnv();
// env.cwd    - دليل مؤقت؛ مرر كـ payload.cwd لالتقاط .failproofai/policies-config.json
// env.home   - دليل منزل معزول؛ لا تسريب ~/.failproofai الحقيقي

env.writeConfig({
  enabledPolicies: ["block-sudo"],
  policyParams: {
    "block-sudo": { allowPatterns: ["sudo systemctl status"] },
  },
});
```

يسجل `createFixtureEnv()` تنظيف `afterEach` تلقائياً.

**`runHook`** - استدعاء الملف الثنائي:

```typescript
import { runHook } from "../helpers/hook-runner";
import { Payloads } from "../helpers/payloads";

const result = await runHook(
  "PreToolUse",
  Payloads.preToolUse.bash("sudo apt install nodejs", env.cwd),
  { homeDir: env.home }
);

expect(result.exitCode).toBe(0);
expect(result.parsed?.hookSpecificOutput?.permissionDecision).toBe("deny");
```

**`Payloads`** - مصانع حمولة جاهزة:

```typescript
Payloads.preToolUse.bash(command, cwd)
Payloads.preToolUse.write(filePath, content, cwd)
Payloads.preToolUse.read(filePath, cwd)
Payloads.postToolUse.bash(command, output, cwd)
Payloads.postToolUse.read(filePath, content, cwd)
Payloads.notification(message, cwd)
Payloads.stop(cwd)
```

### كتابة اختبار E2E

```typescript
import { describe, it, expect } from "vitest";
import { createFixtureEnv } from "../helpers/fixture-env";
import { runHook } from "../helpers/hook-runner";
import { Payloads } from "../helpers/payloads";

describe("block-rm-rf (E2E)", () => {
  it("denies rm -rf", async () => {
    const env = createFixtureEnv();
    env.writeConfig({ enabledPolicies: ["block-rm-rf"] });

    const result = await runHook(
      "PreToolUse",
      Payloads.preToolUse.bash("rm -rf /", env.cwd),
      { homeDir: env.home }
    );

    expect(result.exitCode).toBe(0);
    expect(result.parsed?.hookSpecificOutput?.permissionDecision).toBe("deny");
  });

  it("allows non-recursive rm", async () => {
    const env = createFixtureEnv();
    env.writeConfig({ enabledPolicies: ["block-rm-rf"] });

    const result = await runHook(
      "PreToolUse",
      Payloads.preToolUse.bash("rm /tmp/file.txt", env.cwd),
      { homeDir: env.home }
    );

    expect(result.exitCode).toBe(0);
    expect(result.stdout).toBe("");  // السماح → stdout فارغ
  });
});
```

### أشكال استجابة E2E

| القرار | كود الخروج | stdout |
|----------|-----------|--------|
| `PreToolUse` رفض | `0` | `{"hookSpecificOutput":{"permissionDecision":"deny","permissionDecisionReason":"..."}}` |
| `PostToolUse` رفض | `0` | `{"hookSpecificOutput":{"additionalContext":"Blocked ... because: ..."}}` |
| تعليمات (بخلاف Stop) | `0` | `{"hookSpecificOutput":{"additionalContext":"Instruction from failproofai: ..."}}` |
| تعليمات Stop | `2` | stdout فارغ؛ السبب في stderr |
| السماح | `0` | سلسلة فارغة |

### إعداد Vitest

تستخدم اختبارات E2E `vitest.config.e2e.mts` مع:

- `environment: "node"` - لا توجد متغيرات عامة للمتصفح مطلوبة
- `pool: "forks"` - عزل العملية الحقيقي (الاختبارات تستدعي subprocesses)
- `testTimeout: 20_000` - 20 ثانية لكل اختبار (بدء الملف الثنائي + تقييم الخطاف)

يعتبر حمام `forks` مهماً: تشارك العمال المستندة إلى الخيوط `globalThis`، الأمر الذي قد يتداخل مع اختبارات استدعاء subprocess. تتجنب الشوكات المستندة إلى العملية هذا.

---

## CI

يتعين أن يمر تشغيل CI الكامل (`bun run lint && bunx tsc --noEmit && bun run test:run && bun run build`) قبل الدمج. تعمل مجموعة E2E كوظيفة CI منفصلة بالتوازي.

انظر [المساهمة](../CONTRIBUTING.md) للحصول على قائمة التحقق الكاملة قبل الدمج.