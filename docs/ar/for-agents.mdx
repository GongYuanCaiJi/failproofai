---
title: "للوكلاء"
description: "أضف معرفة Failproof AI إلى وكيل الترميز الخاص بك بأمر واحد. يعمل مع Claude Code و Cursor و Windsurf والمزيد."
---

أضف مرجع Failproof AI الكامل إلى وكيل الترميز الخاص بك بأمر واحد. يعمل مع Claude Code و Cursor و Windsurf وأي وكيل آخر يدعم المهارات.

```bash
npx skills add https://docs.befailproof.ai
```

يكتشف `npx skills` الوكلاء المثبتين لديك ويضيف المهارة بالصيغة الصحيحة لكل واحد منهم تلقائياً.

## ما الذي تغطيه المهارة

| المنطقة | ما يتضمن |
|------|----------------|
| السياسات | أسماء السياسات المدمجة وأنواع الأحداث والمعاملات والتفعيل/التعطيل |
| السياسات المخصصة | `customPolicies.add()` ومرشحات المطابقة وواجهة برمجية `allow`/`deny`/`instruct` |
| كائن السياق | `ctx.eventType` و `ctx.toolName` و `ctx.toolInput` و `ctx.session` |
| التكوين | بنية `policies-config.json` ودمج النطاقات و `policyParams` |
| واجهة سطر الأوامر | `failproofai policies --install` و `--uninstall` و `--custom` والنطاقات |
| لوحة التحكم | عارض الجلسات ونشاط السياسات والمتغيرات البيئية |
| البنية | تدفق معالج الخطاف وأكواد الخروج وعقد stdin/stdout |

## هل المهارة مكتملة؟

ينشئ Mintlify `llms.txt` من جميع الصفحات في التنقل. وتغطي وثائق Failproof AI واجهة برمجية كاملة - كل سياسة وخيار ومثال متضمن. إذا وجدت شيئاً ناقصاً، المصدر موجود في `https://docs.befailproof.ai/llms-full.txt`.

للحصول على سياق مستهدف، قم بربط صفحة معينة مباشرة:

```bash
# واجهة برمجية السياسات المخصصة فقط
npx skills add https://docs.befailproof.ai/custom-policies

# السياسات المدمجة فقط
npx skills add https://docs.befailproof.ai/built-in-policies
```